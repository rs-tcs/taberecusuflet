<?php
if (!$attachmentPosts) return;

foreach ($attachmentPosts as $attachmentPost) {
    echo $this->element('media/admin_add', array(
        'optionId' => $optionId,
        'attachmentPost' => $attachmentPost
    ));
}
?>